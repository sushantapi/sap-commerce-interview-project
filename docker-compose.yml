services:
  solr:
    image: solr:10.0.0
    container_name: electronics-solr
    command:
      - solr-precreate
      - products
    ports:
      - "8983:8983"
    volumes:
      - solr_data:/var/solr
    restart: unless-stopped

volumes:
  solr_data:
